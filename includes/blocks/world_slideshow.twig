{% block world_slideshow %}
  {% if ( fields ) %}
    {% set swiperId = 'swiper-' ~ i ~ random() %}
    <section class="section-history">
      <div class="ct-container slim">
        <h2>{{ fields.title }}</h2>
        <div id="history-globe"></div>
        <div class="swiper-container label-swiper" data-controls=".{{ swiperId }}">
          <div class="swiper-wrapper">
            {% for i in 1..35 %}
              {% if fields['title_milestone_'~i] %}
                <div class="swiper-slide label-slide">{{ fields['date_milestone_'~i] }}</div>
              {% endif %}
            {% endfor %}
          </div>
        </div>
        <div class="swiper-container maxi-swiper maxi-swiper-centered cards-swiper controlled-swiper {{ swiperId }}">
          <div class="swiper-wrapper">
            {% for i in 1..35 %}
              {% if fields['title_milestone_'~i] %}
                <div class="swiper-slide card-explainer" {{ ( fields['lat_milestone_'~i] != '' ) ? ('data-latitude="'~fields['lat_milestone_'~i]~'"')|raw : '' }} {{ ( fields['lon_milestone_'~i] != '' ) ? ('data-longitude="'~fields['lon_milestone_'~i]~'"')|raw : '' }}>
                  <div class="description">
                    <h3>{{ fields['title_milestone_'~i] }}</h3>
                    <p class="subtitle">{{ fields['subtitle_milestone_'~i] }}</p>
                    <p>{{ fields['paragraph_milestone_'~i] }}</p>
                  </div>
                  <div class="thumbnail lazy" data-bg="url({{ fields['img_milestone_'~i] }})"></div>
                </div>
              {% endif %}
            {% endfor %}
          </div>
          <div class="swiper-button-next">
            <span class="icon-chevron-right"></span>
          </div>
          <div class="swiper-button-prev">
            <span class="icon-chevron-right"></span>
          </div>
        </div>
      </div>
    </section>
  {% endif %}
{% endblock %}
