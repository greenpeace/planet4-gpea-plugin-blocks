{% block article_row %}
	{% if ( fields ) %}
	<section class="section-article-row">
		<div class="ct-container">
			<h2><a href="{{ post.link }}">{{ fields.title|e('wp_kses_post')|raw }}<span class="icon-chevron-right"></span></a></h2>
			<div class="swiper-container mini-swiper">
				<div class="swiper-wrapper">
					{% for post in fields.posts %}
					{% if ( fields.display_submit_form and loop.index == 3 ) %}
						<div class="swiper-slide card-mini">
							<p>Want to add your story?</p>
							<p><a href="#" class="button">Click here</a></p>
						</div>
					{% endif %}
						<a href="{{ post.link }}" class="swiper-slide card-mini" data-index="{{ fields.index }}">
							{% if post.img_url %}
								<div
									class="thumbnail lazy"
									data-bg="url({{ post.img_url }})"
								></div>
							{% endif %}
							<div class="meta-box">
								<div>
									<span>{{ post.post_date }}</span>
								</div>
								<div>
									<span>{{ post.news_type }}</span>
								</div>
							</div>
							<p class="caption">{{ post.post_title }}</p>
						</a>
					{% endfor %}
				</div>
			</div>
		</div>
	</section>
	{% endif %}
{% endblock %}
